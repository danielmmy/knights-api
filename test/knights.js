const knights = require('..')
const chai = require('chai')

const expect = chai.expect

describe('REST API', () => {
    describe ('Knights', () => {
        it ('GET /knight/:id')
        it ('POST /knight')
        it ('PUT /knight')
        it ('DELETE /knight/:id')
        it ('LIST /knights')
    })

    describe ('Heroes', () => {
        it ('GET /knights?filter=heroes')
    })
})
